import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    DarkBlue: "#416788",
    Accuamarine: "#58D6BD",
    Blue: "#7389AE",
    LightBlue: "#B5BAD0",
    White: "#E0E0E2",
  },
});

export default theme;
